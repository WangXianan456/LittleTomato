# 2026-09-09 批次 1：Windows 环境验收与测试稳固

依据 `D:\Tomato\LTT\01-GPT-6继续开发任务.md` 的批次 1 执行。保留 `a46a429` 后第二轮未提交成果，本轮未提交、推送或发布。没有新增提醒、设置或美术功能。

## 基线与修复

加载 `scripts/dev-env.ps1` 后确认 Cargo、Rustup、构建、npm、临时目录及开发运行数据均在 D 盘。前端 3 项测试、类型检查和构建通过；调试 EXE 构建与原有 15 项 Windows 烟雾检查通过。

首次 Rust 基线为 **12 通过、1 失败**：`database::tests::failed_upgrade_rolls_back_schema_and_version` 在打开数据库时返回 `database is locked`。检查发现迁移测试仅以系统时间戳命名目录，且使用 `create_dir_all` 允许复用，无法保证同一时钟粒度内并行测试隔离。

本轮只修复测试和验收工具：

- `database.rs`、`timer.rs` 的测试目录增加进程 ID 和原子递增序号，迁移测试用 `create_dir` 拒绝意外重用。
- `scripts/smoke-windows.ps1` 增加 `-ColdStartRounds`，用两个线程在同一启动信号下创建进程，检查唯一存活实例、窗口响应与数据库快照；只清理自己创建的进程。
- 脚本增加实际窗口 DPI、物理尺寸、工作区包含关系以及透明角落/角色/按钮的原生命中检查。
- 测试线程使用 Per-Monitor V2 DPI 上下文，结束时恢复；避免把被 DPI 虚拟化的坐标误当物理坐标。
- 每轮保存 `smoke.log` 与 `recovery.png`。高 DPI 测试还暴露了脚本只读 SQLite 连接没有 busy timeout 的问题，补上 5 秒等待后重跑通过。

现有窗口、单实例和系统事件业务逻辑保持原实现，本批实测没有暴露需要修改这些逻辑的缺陷。

## 自动验证结果

- `npm test`：3/3。
- `npm run check`、`npm run build`：通过。
- 修复后 `cargo test --locked --manifest-path src-tauri/Cargo.toml`：13/13。
- 两项迁移测试并行重复运行 10 轮：全部通过。
- `cargo fmt --check`、`cargo clippy --locked -- -D warnings`：通过。
- `npm run tauri build -- --debug --no-bundle`：通过并启动实际 EXE。

## 同时冷启动

命令：`./scripts/smoke-windows.ps1 -ColdStartRounds 10`。

10 轮均通过，每轮只留下一个可响应主窗口，独立数据库可读且计时为 ready。之后原有 15 项完整烟雾检查通过，共 45 个断言通过。此结果是本机 10 轮实测，不代表证明所有进程调度条件下都不存在竞争。

证据：`D:\Coding\Runtime\LittleTomato-smoke-fed54cc414a44f0b893b0f30070e922f`，含每轮独立 `cold-*` 数据库及 `smoke.log`。

## 真实系统缩放

通过 Windows“设置 → 系统 → 屏幕 → 缩放”实际切换比例，未使用浏览器缩放代替。显示器分辨率 1920×1080。

| 系统缩放 | GetDpiForWindow | 窗口物理位置 | 物理尺寸 | 结果 |
|---|---|---|---|---|
| 100% | 96 | (1608, 600) | 300×420 | 20/20 |
| 125% | 120 | (1530, 480) | 375×525 | 20/20 |
| 150% | 144 | (1452, 360) | 450×630 | 20/20 |

每轮检查：工作区内可见、实际鼠标命中/穿透、点击开始、重复启动、模拟会话与电源消息、恢复确认、关闭隐藏和再次启动唤回。目视检查截图：气泡、角色、计时卡和底部统计未裁切，恢复提示可读。这里的系统事件仍为发送到真实窗口的模拟消息，不能视为真实锁屏或硬件睡眠验收。

证据目录：

- 100%：`D:\Coding\Runtime\LittleTomato-smoke-9a4fc6ef05764f63bd88e8c536890bc0`
- 125%：`D:\Coding\Runtime\LittleTomato-smoke-bf527d3623d04018a26583ce9ec78a42`
- 150%：`D:\Coding\Runtime\LittleTomato-smoke-d4e4901e3b4d40869b4bb0e030e362ff`

125% 首次尝试在脚本读取活动数据库时因无 busy timeout 中止；上表为修复后的完整重跑结果。测试完成已恢复原始 100% 系统缩放并关闭本轮打开的设置窗口。未改变用户日常数据库内容来构造测试。

## 未验收与发布阻断项

- **真实锁屏/睡眠**：已询问是否可配合解锁/唤醒，尚未收到答复，没有擅自锁定或休眠工作站。`powercfg /a` 显示支持 S3；有硬件能力不等于已验收。
- **200% DPI**：本机标准缩放列表只提供 100%、125%、150%、175%。未使用需要注销的自定义缩放。列表证据：`D:\Coding\Temp\LittleTomato\available-scales.txt`。
- **双屏、负坐标、拔插、跨屏 DPI 和运行中切换缩放**：仅一块可用显示器。本轮覆盖不同系统 DPI 下启动与交互，不能外推为跨屏或运行中 DPI 切换已验收。
- **真实远程会话断开/重连**：没有可控的独立测试会话，未断开当前工作连接。
- **Explorer 重启**：尝试打开任务管理器但未出现可交互主窗口，没有执行 Explorer 重启；未强制结束用户现有 Explorer 进程。该项保持未验收。

以上继续作为发布前验收阻断标签。现有证据没有显示窗口丢失或错误累计时间的严重缺陷，按任务书允许继续独立的批次 2。

## 下一步

进入“批次 2：完整提醒闭环”：稳定完成事件 ID、持久化最新待办、延后 5/10/15 分钟、可见/隐藏/全屏投递路径和通知点击唤回。本轮不提前实现这些业务功能。
